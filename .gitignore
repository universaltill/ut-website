node_modules/

# Playwright test run output (site/ has no build step; this is just test
# tooling scratch space, ut-docs#458).
test-results/
playwright-report/

# Build output. `npm run build` (Astro) generates this from scratch every
# time — site/ passes through byte-for-byte (astro.config.mjs
# publicDir: 'site') plus the blog/plugins pages Astro renders — so it's
# never something to commit.
dist/
.astro/
